<% provide(:title, '売り手ユーザーリスト') %>
<div class = "container">
  <div class="row">
    <div class="col-md-1">
    </div>
    <div class="col-md-10">
    <h3> 売り手ユーザーリスト</h3>
      <table class="table">
        <tr>
          <th>ユーザNo.</th><th>姓</th><th>名</th><th>メールアドレス</th><th>編集</th><th>削除</th>
        </tr>
        <% @s_users.each do |s_user| %>
        <tr>
          <td><%= s_user.id %></td>
          <td><%= s_user.fname %></td>
          <td><%= s_user.gname %></td>
          <td><%= s_user.email %></td>
          <td><%= link_to '編集', edit_user_registration_path(s_user) %></td>
          <td><%= link_to '削除', s_user, method: :delete, data: {confirm: '本当に削除しますか？'} %></td>
        </tr>
        <% end %>
      </table>
    </div>
    <div class="col-md-1">
    </div>
    </div>
  </div>
</div>